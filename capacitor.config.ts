import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bienestar.app',
  appName: 'Bienestar Universitario',
  webDir: 'dist'
};

export default config;
